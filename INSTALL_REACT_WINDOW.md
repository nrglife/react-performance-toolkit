# Install react-window

Run the following command to install react-window for the virtualized list demo:

```bash
npm install react-window
npm install --save-dev @types/react-window
```

This library is used in the VirtualizedDemo to efficiently render large lists by only rendering visible items.
